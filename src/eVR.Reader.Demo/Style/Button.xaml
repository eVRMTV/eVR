<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Fonts.xaml"/>
        <ResourceDictionary Source="Colors.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="Button">
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Top" />
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="MinHeight" Value="25" />
        <Setter Property="MinWidth" Value="100" />
        <Setter Property="Padding" Value="5,0,5,0" />
        <Setter Property="Margin" Value="10,5" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="FontFamily" Value="{StaticResource SansSerif}"/>
        <Setter Property="Foreground" Value="{StaticResource WhiteCB}"/>
        <Setter Property="Background" Value="{StaticResource BlackCB}"/>

        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{StaticResource GreyCB}"/>
                <Setter Property="Foreground" Value="{StaticResource BlackCB}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
    